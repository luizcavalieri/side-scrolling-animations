'use strict';

import SideSliderMagicscroll from '../side-slider-magicscroll';

describe('SideSliderMagicscroll View', function() {

  beforeEach(() => {
    this.sideSliderMagicscroll = new SideSliderMagicscroll();
  });

  it('Should run a few assertions', () => {
    expect(this.sideSliderMagicscroll).toBeDefined();
  });

});
